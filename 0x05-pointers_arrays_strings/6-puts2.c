#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * puts2 - Entry point
 *
 * Description: Prints every character of a string
 * @str: Pointer to a char
 * Return: void
 */

void puts2(char *str)
{
	int i, input;

	for (i = 0; i < strlen(input); i += 2)
	{
		_putchar(input[i]);
	}
}
